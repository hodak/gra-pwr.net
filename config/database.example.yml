default: &default
  adapter: postgresql
  host: localhost
  encoding: utf8
  pool: 5
  timeout: 5000
  encoding: utf8
  username:
  password:

development:
  <<: *default
  database: infish_development

test:
  <<: *default
  database: infish_test

production:
  <<: *default
  database: infish_production
  username:
  password:

