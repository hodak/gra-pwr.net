machine:
  services:
    - docker

dependencies:
  override:
    - docker info
    - docker build -t hodak/infish .

database:
  override:
    - /bin/true

test:
  override:
    - docker run hodak/infish bundle exec rspec
